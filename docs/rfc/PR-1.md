# PR-1 — Bootstrap

**Status:** Implemented

## Цель

Создать полностью независимый проект Voice Assistant MVP: собирается
отдельно, запускается отдельно, не зависит от Driver UI.

## Definition of Done

- проект устанавливается, собирается, запускается;
- ESLint без ошибок;
- TypeScript компилируется без предупреждений;
- тестовая инфраструктура запускается.

## Не входит в PR-1

Voice SDK API (PR-2), Voice Emulator (PR-7), Demo UI (PR-9), Unit Tests.